# ── Vercel frontend → backend wiring ──────────────────────────
# Set this in Vercel: Project → Settings → Environment Variables.
# It is the public URL of your deployed FastAPI backend (no trailing slash).
# The browser calls ${NEXT_PUBLIC_API_BASE}/api/* directly.
NEXT_PUBLIC_API_BASE=https://your-backend-host.example.com
