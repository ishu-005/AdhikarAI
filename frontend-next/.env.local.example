# In dev, point the UI at the FastAPI backend (CORS is open by default).
# In the single-container production build (static export served by FastAPI),
# leave this empty so the UI uses same-origin relative /api paths.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
